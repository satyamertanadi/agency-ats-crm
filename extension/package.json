{
  "name": "agency-ats-extension",
  "private": true,
  "version": "0.1.0",
  "description": "Chrome (MV3) extension to capture LinkedIn profiles into Agency ATS.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "lint": "node ../node_modules/eslint/bin/eslint.js src build.mjs vitest.config.ts --max-warnings 0",
    "test": "node ../node_modules/vitest/vitest.mjs run --config vitest.config.ts",
    "typecheck": "node ../node_modules/typescript/bin/tsc --noEmit"
  }
}
