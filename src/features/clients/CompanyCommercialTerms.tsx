import {useState} from 'react'
import {useMutation,useQuery,useQueryClient} from '@tanstack/react-query'
import {formatMoney} from '../../shared/lib/format'
import {Button} from '../../shared/ui/Button'
import {Field,Input,Select,Textarea} from '../../shared/ui/Field'
import {Badge,Panel} from '../../shared/ui/Page'
import {useToast} from '../../shared/ui/Toast'
import {ErrorState,LoadingState} from '../../shared/ui/States'
import {listCompanyCommercialTerms,setCompanyDefaultFee} from '../core/commercialRepository'

export function CompanyCommercialTerms({organizationId,companyId,baseCurrency,canEdit}:{organizationId:string;companyId:string;baseCurrency:string;canEdit:boolean}){
  const cache=useQueryClient();const [editing,setEditing]=useState(false);const [feeType,setFeeType]=useState<'percentage'|'fixed'>('percentage');const [amount,setAmount]=useState('');const [currency,setCurrency]=useState(baseCurrency);const [guaranteeDays,setGuaranteeDays]=useState('90')
  const [paymentTermsDays,setPaymentTermsDays]=useState('');const [taxTreatment,setTaxTreatment]=useState('');const [replacementTerms,setReplacementTerms]=useState('');const [documentUrl,setDocumentUrl]=useState('');const [approvalStatus,setApprovalStatus]=useState<'draft'|'pending_approval'|'approved'|'rejected'>('approved');const [notes,setNotes]=useState('')
  const toast=useToast()
  const query=useQuery({queryKey:['company-commercial-terms',organizationId,companyId],queryFn:()=>listCompanyCommercialTerms(organizationId,companyId)})
  const mutation=useMutation({mutationFn:()=>setCompanyDefaultFee(organizationId,companyId,{feeType,feePercentage:feeType==='percentage'?Number(amount):undefined,fixedFee:feeType==='fixed'?Number(amount):undefined,currency,guaranteeDays:Number(guaranteeDays),paymentTermsDays:paymentTermsDays?Number(paymentTermsDays):undefined,taxTreatment:taxTreatment||undefined,replacementTerms:replacementTerms||undefined,agreementDocumentUrl:documentUrl||undefined,approvalStatus,notes:notes||undefined}),onSuccess:async()=>{setEditing(false);await Promise.all([cache.invalidateQueries({queryKey:['company-commercial-terms',organizationId,companyId]}),cache.invalidateQueries({queryKey:['job-health',organizationId]}),cache.invalidateQueries({queryKey:['company-pipeline',organizationId]})]);toast.success('Account fee agreement updated.','The previous agreement was retained as history, not overwritten.')},onError:(error)=>toast.error(error,'The agreement was not changed.')})
  if(query.isLoading)return <LoadingState label="Loading commercial terms…"/>;if(query.error)return <ErrorState error={query.error}/>
  const active=query.data?.find((term)=>term.status==='active')
  return <Panel title="Commercial terms" subtitle="Account-level defaults flow into job health unless a job has its own fee override." action={canEdit?<Button variant="secondary" onClick={()=>setEditing((value)=>!value)}>{editing?'Cancel':'Set defaults'}</Button>:undefined}>
    {editing?<form className="form-grid" onSubmit={(event)=>{event.preventDefault();mutation.mutate()}}><Field label="Fee type"><Select value={feeType} onChange={(event)=>setFeeType(event.target.value as 'percentage'|'fixed')}><option value="percentage">Percentage</option><option value="fixed">Fixed fee</option></Select></Field><Field label={feeType==='percentage'?'Percentage':'Amount'}><Input type="number" min="0" max={feeType==='percentage'?100:undefined} step={feeType==='percentage'?'0.001':'0.01'} value={amount} onChange={(event)=>setAmount(event.target.value)} required/></Field><Field label="Currency"><Input maxLength={3} value={currency} onChange={(event)=>setCurrency(event.target.value.toUpperCase())} required/></Field><Field label="Guarantee days"><Input type="number" min="0" max="730" value={guaranteeDays} onChange={(event)=>setGuaranteeDays(event.target.value)} required/></Field><Field label="Payment terms (days)"><Input type="number" min="0" max="365" value={paymentTermsDays} onChange={(event)=>setPaymentTermsDays(event.target.value)}/></Field><Field label="Tax treatment"><Input value={taxTreatment} onChange={(event)=>setTaxTreatment(event.target.value)} placeholder="e.g. VAT exclusive"/></Field><Field label="Approval status"><Select value={approvalStatus} onChange={(event)=>setApprovalStatus(event.target.value as typeof approvalStatus)}><option value="draft">Draft</option><option value="pending_approval">Pending approval</option><option value="approved">Approved</option><option value="rejected">Rejected</option></Select></Field><Field label="Agreement document URL"><Input type="url" value={documentUrl} onChange={(event)=>setDocumentUrl(event.target.value)}/></Field><div className="full"><Field label="Replacement conditions"><Textarea value={replacementTerms} onChange={(event)=>setReplacementTerms(event.target.value)} rows={2}/></Field></div><div className="full"><Field label="Notes"><Textarea value={notes} onChange={(event)=>setNotes(event.target.value)} rows={2}/></Field></div>{mutation.error&&<p className="form-error full" role="alert">{mutation.error.message}</p>}<div className="form-actions full"><Button loading={mutation.isPending} disabled={!amount||currency.length!==3}>Save defaults</Button></div></form>:active?<dl className="record-summary"><div><dt>Fee</dt><dd>{active.fee_type==='percentage'?`${active.fee_percentage}%`:formatMoney(active.fixed_fee,active.currency)}</dd></div><div><dt>Guarantee</dt><dd>{active.guarantee_days} days</dd></div><div><dt>Effective from</dt><dd>{active.effective_from}</dd></div><div><dt>Source</dt><dd>Account agreement</dd></div><div><dt>Payment terms</dt><dd>{active.payment_terms_days!=null?`${active.payment_terms_days} days`:'Not recorded'}</dd></div><div><dt>Tax treatment</dt><dd>{active.tax_treatment||'Not recorded'}</dd></div><div><dt>Approval</dt><dd>{active.approval_status==='approved'?<Badge tone="good">Approved</Badge>:active.approval_status==='rejected'?<Badge tone="bad">Rejected</Badge>:<Badge tone="warn">{active.approval_status==='pending_approval'?'Pending approval':'Draft'}</Badge>}</dd></div>{active.replacement_terms&&<div className="full"><dt>Replacement conditions</dt><dd>{active.replacement_terms}</dd></div>}{active.agreement_document_url&&<div><dt>Agreement</dt><dd><a className="record-link" href={active.agreement_document_url} target="_blank" rel="noreferrer">Open document</a></dd></div>}</dl>:<p className="muted">No account-level fee defaults recorded. Job-specific fees still apply.</p>}
  </Panel>
}
